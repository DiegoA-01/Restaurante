spring:
  application:
    name: restaurantediego

  datasource:
    url: dbc:mysql://localhost:3306/pos_db
    username: root
    password: Di1004960034.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
      "[format_sql]": true

server:
  port: 8080
  servlet:
    context-path: /api/v1

logging:
  level:
    "[org.springframework]": INFO
